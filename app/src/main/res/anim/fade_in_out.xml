<?xml version="1.0" encoding="utf-8"?>
<!--
  Fade animation for the loading logo.
  The Z.ai logo stays upright (no rotation) and gently pulses in and out,
  creating a calm "thinking" effect. Loops infinitely until the page loads.
-->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:shareInterpolator="false">

    <!-- Fade in and out cycle -->
    <alpha
        android:fromAlpha="0.35"
        android:toAlpha="1.0"
        android:duration="1200"
        android:repeatCount="infinite"
        android:repeatMode="reverse"
        android:interpolator="@android:interpolator/accelerate_decelerate" />

</set>
